import React from "react";
import styled from "styled-components";

export const LetsText = () => {
  return <Wrapper></Wrapper>;
};

const Wrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  height: calc(100vh - 10rem);
`;
